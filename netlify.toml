[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "18"
  NPM_FLAGS = "--legacy-peer-deps"

[context.production]
  command = "npm run build"

[context.production.environment]
  NPM_FLAGS = "--legacy-peer-deps"